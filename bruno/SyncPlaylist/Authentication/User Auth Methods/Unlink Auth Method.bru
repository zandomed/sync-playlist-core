meta {
  name: Unlink Auth Method
  type: http
  seq: 10
}

delete {
  url: {{URL}}/auth/unlink/google?userId=user123
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("should unlink auth method successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
  });
}