meta {
  name: Get User Auth Methods
  type: http
  seq: 8
}

get {
  url: {{URL}}/auth/methods/user123
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("should return user auth methods", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().providers).to.be.an('array');
  });
}