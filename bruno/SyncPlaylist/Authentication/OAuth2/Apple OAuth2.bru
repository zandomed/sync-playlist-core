meta {
  name: Apple OAuth2
  type: http
  seq: 4
}

get {
  url: {{URL}}/auth/oauth2/apple
  body: none
  auth: none
}

tests {
  test("should return OAuth2 URL for Apple", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().authUrl).to.be.a('string');
    expect(res.getBody().authUrl).to.contain('/oauth2/authorization/apple');
    expect(res.getBody().provider).to.equal('apple');
  });
}